import React, { useEffect, useState } from 'react';
import { api } from '@/src/services/api';
import { Edit3, ShieldCheck, UserRound } from 'lucide-react';
import { EmployeePage, Stats } from './EmployeeModuleShared';
export const EmployeeProfileView: React.FC = () => { const [profile, setProfile] = useState<any>(null); const [error, setError] = useState(''); const load = async () => { const response = await api.get('/employee/profile'); if (response.success) setProfile(response.data); else setError(response.message || 'Unable to load profile.'); }; useEffect(() => { load(); }, []); const updatePhone = async () => { const phone = window.prompt('Phone number', profile?.phone || ''); if (!phone) return; const response = await api.put('/employee/profile', { phone }); if (response.success) await load(); }; return <EmployeePage onAction={updatePhone} eyebrow="Identity workspace" title="My Profile" description="Keep your employee profile, contact details and security preferences current." icon={UserRound} action="Edit profile">{error && <div className="rounded-xl bg-amber-50 p-3 text-xs font-semibold text-amber-700">{error}</div>}<div className="grid grid-cols-1 gap-6 lg:grid-cols-[280px_minmax(0,1fr)]"><aside className="rounded-2xl border border-slate-200 bg-white p-6 text-center shadow-sm"><div className="mx-auto flex h-24 w-24 items-center justify-center rounded-3xl bg-blue-600 text-2xl font-black text-white">{(profile?.avatar || profile?.name || 'AS').slice(0, 2).toUpperCase()}</div><h2 className="mt-4 text-lg font-black text-slate-900">{profile?.name || 'Loading profile...'}</h2><p className="mt-1 text-xs text-slate-500">{profile?.designation || 'Employee'}</p><span className="mt-4 inline-flex rounded-full bg-emerald-50 px-3 py-1 text-[10px] font-bold text-emerald-700">Active employee</span><button onClick={updatePhone} className="mt-6 flex w-full items-center justify-center gap-2 rounded-xl border border-slate-200 py-2.5 text-xs font-bold text-slate-600"><Edit3 className="h-3.5 w-3.5" /> Edit phone</button></aside><section className="space-y-4"><div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="text-sm font-black text-slate-900">Personal information</h2><div className="mt-5 grid grid-cols-1 gap-5 sm:grid-cols-2">{[['Full name', profile?.name], ['Email', profile?.email], ['Employee ID', profile?.employeeCode], ['Department', profile?.department], ['Phone', profile?.phone], ['Joining date', profile?.joiningDate]].map(([label, value]) => <div key={String(label)}><p className="text-[10px] font-bold uppercase tracking-wider text-slate-400">{label}</p><p className="mt-1 text-sm font-semibold text-slate-800">{value || 'Loading...'}</p></div>)}</div></div><div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center gap-2"><ShieldCheck className="h-5 w-5 text-emerald-600" /><h2 className="text-sm font-black text-slate-900">Security & access</h2></div><div className="mt-4 flex flex-wrap gap-3"><button className="rounded-xl border border-slate-200 px-3 py-2 text-xs font-bold text-slate-600">Change password</button><button className="rounded-xl bg-emerald-50 px-3 py-2 text-xs font-bold text-emerald-700">2FA enabled</button></div></div></section></div></EmployeePage>; };
