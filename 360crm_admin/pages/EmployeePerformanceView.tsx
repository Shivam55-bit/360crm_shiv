import React, { useEffect, useState } from 'react';
import { api } from '@/src/services/api';
import { Award, BarChart3, Target, TrendingUp } from 'lucide-react';
import { EmployeePage, Stats } from './EmployeeModuleShared';
const points = [28, 42, 36, 58, 49, 72];
export const EmployeePerformanceView: React.FC = () => { const [metrics, setMetrics] = useState<any>(null); const [error, setError] = useState(''); const load = async () => { const response = await api.get('/employee/performance'); if (response.success) setMetrics(response.data); else setError(response.message || 'Unable to load performance data.'); }; useEffect(() => { load(); }, []); const conversion = metrics?.conversionRate || '0%'; return <EmployeePage onAction={load} eyebrow="Growth workspace" title="My Performance" description="See the activity and outcomes behind your monthly performance." icon={TrendingUp} action="Refresh analytics">{error && <div className="rounded-xl bg-amber-50 p-3 text-xs font-semibold text-amber-700">{error} Showing current workspace defaults.</div>}<Stats items={[{ label: 'Monthly sales', value: metrics?.totalQuotedValue ? `₹${Number(metrics.totalQuotedValue).toLocaleString('en-IN')}` : '₹0', detail: 'Live quoted value', tone: 'text-blue-600' }, { label: 'Target', value: '₹8.0L', detail: '85% achieved', tone: 'text-amber-600' }, { label: 'Conversion rate', value: conversion, detail: 'From assigned leads', tone: 'text-emerald-600' }, { label: 'Leads closed', value: String(metrics?.convertedLeads ?? 0), detail: `${metrics?.totalCalls ?? 0} calls logged`, tone: 'text-blue-600' }]} /><div className="grid grid-cols-1 gap-6 lg:grid-cols-[minmax(0,1fr)_320px]"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><div><h2 className="text-sm font-black text-slate-900">Monthly activity</h2><p className="mt-1 text-xs text-slate-500">Sales achievement over the last six months</p></div><BarChart3 className="h-5 w-5 text-blue-600" /></div><div className="mt-6 flex h-52 items-end gap-3">{points.map((point, index) => <div key={index} className="flex flex-1 flex-col items-center gap-2"><div className="w-full rounded-t-xl bg-blue-600 transition-all hover:bg-blue-500" style={{ height: `${point * 2.2}px` }} /><span className="text-[10px] text-slate-400">{['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun'][index]}</span></div>)}</div></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center gap-2"><Award className="h-5 w-5 text-amber-500" /><h2 className="text-sm font-black text-slate-900">Goal completion</h2></div><div className="mx-auto mt-6 flex h-36 w-36 items-center justify-center rounded-full" style={{ background: 'conic-gradient(#2563eb 0 85%, #e2e8f0 85% 100%)' }}><div className="flex h-28 w-28 flex-col items-center justify-center rounded-full bg-white"><span className="text-2xl font-black text-slate-900">85%</span><span className="text-[10px] text-slate-400">achieved</span></div></div><div className="mt-5 space-y-3 text-xs"><p className="flex items-center justify-between"><span className="flex items-center gap-2 text-slate-500"><Target className="h-3.5 w-3.5" />Monthly target</span><b>₹8.0L</b></p><p className="flex items-center justify-between"><span className="flex items-center gap-2 text-slate-500"><TrendingUp className="h-3.5 w-3.5" />Current quoted value</span><b className="text-blue-600">₹{Number(metrics?.totalQuotedValue || 0).toLocaleString('en-IN')}</b></p></div></section></div></EmployeePage>; };
